// Ituimanager
// showthinking(string, {timeoutMs, showall (weather to truncate to one line or show full)}) // this shows thinking process it will be dark and after some timeout it will internally calls clar
// private clear() this will clear the thinking rendered string
// private calculate truncate length
// output(string) permanent show the text
// input(placeholder:string) ask for user input
// private useroutput() only if after giving input user text will be deleted then use this to render text.